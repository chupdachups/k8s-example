apiVersion: v1
kind: Pod
metadata:
  name: kubernetes-bootstrap-yaml
spec:
  containers:
  - name: kubernetes-bootstrap
    image: gcr.io/google-samples/kubernetes-bootcamp:v1
    ports:
    - containerPort: 8080